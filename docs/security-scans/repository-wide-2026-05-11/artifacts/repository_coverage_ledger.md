# Repository Coverage Ledger

| Row | Boundary / shard | Family | Files checked | Disposition | Evidence summary |
|---|---|---|---|---|---|
| RW-001 | Startup seed data | Default credentials / secret material | `Program.cs`, `ApplicationBuilderExtensions.cs`, `ApplicationDbContextSeed.cs` | reportable | Startup calls database seeding and seed contains fixed admin/viewer passwords plus `default-api-key`. |
| RW-002 | Privileged authorization API | Authz bypass / privilege escalation | `UsersController.cs`, `RolesController.cs`, `PermissionsController.cs`, `ApplicationsController.cs`, `TenantsController.cs`, `PermissionPolicyProvider.cs`, `PermissionAuthorizationHandler.cs` | reportable | Permission policy infrastructure exists, but no runtime endpoint uses permission policies or `AuthorizeAsync`; controllers only require any authenticated principal. |
| RW-003 | Tenant-aware identity data | Tenant/object isolation | `TenantResolutionMiddleware.cs`, `CurrentUserService.cs`, `User.cs`, user/tenant/application controllers | reportable | Tenant slug/claim state exists, but controller queries do not filter by tenant or compare target object tenant against current principal. |
| RW-004 | Login endpoint | Rate limiting / credential stuffing | `AuthController.cs`, `LoginCommand.cs`, `User.cs`, `appsettings.json`, service registration | reportable | README/config advertise rate limiting, but no rate-limiter middleware/service is wired; lockout only records failed attempts for existing users after user lookup. |
| RW-005 | JWT bearer setup | Token transport leakage | `ServiceCollectionExtensions.cs` | reportable | JWT bearer handler accepts `access_token` from query string for all requests, not restricted to WebSocket/SSE paths. |
| RW-006 | EF Core query paths | SQL injection | Controllers, application commands, persistence configs | suppressed | Queries use LINQ/EF APIs and no `FromSql`, `ExecuteSql`, or raw SQL sink was found in runtime code. |
| RW-007 | File/path APIs | Path traversal / arbitrary file read | `Program.cs`, `JwtService.cs`, `SwaggerExtensions.cs` | suppressed | File reads are from operator-controlled JWT key paths or static XML documentation path, not attacker-controlled request input. |
| RW-008 | Network/process/template/parser sinks | SSRF/RCE/deserialization/SSTI/XXE | Full `src/**` pass | not_applicable | No attacker-controlled network fetch, process execution, template rendering, XML parser, or unsafe deserializer sink was found in runtime code. |
| RW-009 | Dependency advisory audit | Third-party CVEs | project files | deferred | No CVE/GHSA seed was provided and package restore/build was blocked by sandboxed network access to NuGet. This scan is source-code focused, not a complete dependency advisory audit. |
